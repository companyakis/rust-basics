#[allow(unused_variables)]

fn main() {

    let v1: Vec<u8> = Vec::new();
    
    let mut v2 = vec![100, 200, 300];
    
    let v3: Vec<i16> = vec![-10, -345, 0, 12];
    
    let names: Vec<&str> = vec!["Mustafa", "Kutluk", "Bilge", "Bumin", "Kültigin"];
}
